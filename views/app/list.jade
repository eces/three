extends ../layout
include ../platform-mixin

block content
  div.container
    div.row
      div.col-xs-9
        table.table
          thead
              tr
                th #
                th 플랫폼
                th 이름
                th 식별자
                th 변경일자
                th 
            tbody
              each app in apps
                tr
                  td= app.id
                  td
                    +platform(app.platform)
                  td= app.name
                  td= app.identifier
                  td #{moment(app.updatedAt).calendar()}
                  td
                    a.btn.btn-embossed.mrm.btn-sm.btn-default(href='/apps/'+app.id) 정보 수정
              td(colspan=6)
                p #{apps.length}개의 앱

      div.col-xs-3
        button.btn.btn-primary.btn-block(data-bind='click: createApp') 새로운 앱 추가하기
        br
        hr
        h6 도움말
        a(href='/docs/apps/overview') 앱 서비스에 대해 알아보기
        br
        a(href='/docs/apps/create') 새로 만들기

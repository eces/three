extends ../layout
include ../platform-mixin

block content
  div.container
    div.row
      div.col-xs-9
        table.table
          thead
              tr
                th #
                th 플랫폼
                th 앱
                th 이름
                th 변경일자
                th 
            tbody
              each board in boards
                tr
                  td= board.id
                  td
                    +platform(board.appPlatform)
                  td= board.appName
                  td= board.name
                  td #{moment(board.updatedAt).calendar()}
                  td(width='140px')
                    div.btn-group
                      a.btn.btn-embossed.btn-sm.btn-default(href='/boards/'+board.id) 수정
                      a.btn.btn-embossed.btn-sm.btn-primary(href='/boards/'+board.id+'/posts') 게시물 관리
              td(colspan=6)
                p #{boards.length}개의 게시판

      div.col-xs-3
        button.btn.btn-primary.btn-block(data-bind='click: createBoard') 새로운 게시판 추가하기
        br
        hr
        h6 도움말
        a(href='/docs/board/overview') 게시판에 대해 알아보기
        br
        a(href='/docs/board/create') 새로 만들기